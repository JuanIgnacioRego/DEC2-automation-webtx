[unittest]
plugins = nose2.plugins.junitxml

[junit-xml]
always-on = True
keep_restricted = False
path = reports/test_report_1542415333.xml
test_fullname = True

    ).format("/build/" if os.getenv("ENVIRONMENT", defaultEnvironment) == "jenkins" else "",
            os.getenv("BUILDVERSION",str(int(time.time()))))
            )
        